{
  "name": "cyberchef-platform",
  "private": true,
  "workspaces": ["apps/*", "services/*", "packages/*"],
  "scripts": {
    "dev": "echo 'Run services individually'",
    "build": "echo 'Build all services'",
    "start": "docker-compose -f infrastructure/docker-compose.yml up"
  },
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^7.0.0",
    "jsonwebtoken": "^9.0.0",
    "bcrypt": "^5.1.0",
    "stripe": "^12.0.0"
  }
}
